# 4.1 Create a custom view [UI]

## Task

Create a custom view that displays the trace in a way that is easier to read, and that lets the viewer take write actions to annotate the trace. This is open ended, so design it for a persona that will be doing the reviewing. In the trace viewer, navigate to the Views tab, and prompt Loop to generate this new view. This may likely take a few iterations.
