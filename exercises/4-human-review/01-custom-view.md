# 5.1 Create a custom view [UI]

## Task

Create a custom view that displays the trace in a way that is easier to read, and
that lets the viewer take write actions to annotate the trace. This is open ended,
so design it for whoever will be doing the reviewing.
